import { searchTags } from 'ember-app-qa/helpers/search-tags';
import { module, test } from 'qunit';

module('Unit | Helper | search tags');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = searchTags([42]);
  assert.ok(result);
});
